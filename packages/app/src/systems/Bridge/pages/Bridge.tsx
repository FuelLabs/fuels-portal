import { Text } from '@fuel-ui/react';

import { Layout } from '~/systems/Core';

export const Bridge = () => {
  return (
    <Layout>
      <Text>Bridge</Text>
      <Bridge />
    </Layout>
  );
};
